(function() {
  jQuery(function() {
    $('header h1').fitText(0.59, {
      maxFontSize: '102px'
    });
    $('header p').fitText(2.5, {
      minFontSize: '20px',
      maxFontSize: '1.6em'
    });
    return $('a').attr('target', '_blank');
  });

}).call(this);
